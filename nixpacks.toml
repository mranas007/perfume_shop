providers = ["python"]

[phases.setup]
nixPkgs = [
  "python311Full",
  "postgresql",
  "gcc"
]

[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "python manage.py collectstatic --noinput"
]

[start]
cmd = "honcho start"